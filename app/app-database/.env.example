# PostgreSQL Configuration
POSTGRES_USER=andi_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=andi_db
POSTGRES_PORT=5432

# PgAdmin Configuration
PGADMIN_EMAIL=admin@andi.local
PGADMIN_PASSWORD=change_me_in_production
PGADMIN_PORT=5050

# Connection String (for application use)
DATABASE_URL=postgresql://andi_user:change_me_in_production@localhost:5432/andi_db

# Azure Database Configuration (for production)
AZURE_POSTGRES_HOST=
AZURE_POSTGRES_USER=
AZURE_POSTGRES_PASSWORD=
AZURE_POSTGRES_DB=
AZURE_POSTGRES_SSL_MODE=require

# Backup Configuration
BACKUP_RETENTION_DAYS=7
BACKUP_SCHEDULE="0 2 * * *"  # 2 AM daily

# Connection Pool Settings
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_CONNECTION_TIMEOUT=30000

# Environment
NODE_ENV=development